================================================================================
GENERAL SETTINGS — FULL IMPLEMENTATION TODO LIST
For use by an AI coding agent. Each task describes WHAT to do and WHICH files/
functions must be created or updated.
================================================================================

STATUS LEGEND:  [DONE] = already implemented   [TODO] = not yet done

--------------------------------------------------------------------------------
TASK 1  [DONE]  Backend: API endpoints for General Settings
--------------------------------------------------------------------------------
File:    backend/src/routes/settings.ts
What was done:
  - Added helper functions: loadGeneralSettings(), saveGeneralSettings()
  - Persists to: backend/data/general-settings.json
  - GET  /api/settings/general  — returns current settings (with defaults)
  - PUT  /api/settings/general  — saves validated patch

Defaults stored:
  companyName       = ""
  language          = "en"
  timeZone          = "Asia/Dhaka"
  dateFormat        = "DD/MM/YYYY"
  timeFormat        = "12h"
  theme             = "light"

  NOTE: dbRetentionMonths was originally stored here but has since been moved
  to db-config.json and managed by the Database Settings page (see TASK 14).


--------------------------------------------------------------------------------
TASK 2  [DONE]  Frontend: General Settings Page UI
--------------------------------------------------------------------------------
File:    frontend/src/app/settings/general/page.tsx
What was done:
  - Created full page with 5 sections: Company, Regional, Date/Time, Appearance,
    Data Retention
  - Loads settings from GET /api/settings/general on mount
  - Saves via PUT /api/settings/general on "Save Settings" button click
  - Shows success/error alerts
  - Follows the same design pattern as SystemSettingsPage
  - Calls refreshGlobalSettings() on save to propagate changes app-wide


--------------------------------------------------------------------------------
TASK 3  [DONE]  Frontend: Settings Index Page — Add General Settings card
--------------------------------------------------------------------------------
File:    frontend/src/app/settings/page.tsx
What was done:
  - Added General Settings entry to the settingsOptions array (first card)
  - Added 'rose' color key to getColorClasses()


--------------------------------------------------------------------------------
TASK 4  [DONE]  Frontend: Sidebar — Add General Settings to hover dropdown
--------------------------------------------------------------------------------
File:    frontend/src/components/Sidebar.tsx
What was done:
  - Prepended General Settings to the settingsItems array so it appears at the
    top of the Settings hover panel


--------------------------------------------------------------------------------
TASK 5  [TODO]  Apply Language Setting across the application
--------------------------------------------------------------------------------
What needs to be done:
  - Install an i18n library (recommended: next-intl or react-i18next)
  - Create locale files:
      frontend/src/locales/en.json
      frontend/src/locales/bn.json   (and others as needed)
  - Create a LanguageContext (or use next-intl provider) that:
      * Reads the language setting from GET /api/settings/general on app startup
      * Stores the active locale in context
      * Provides a useTranslation() / t() hook to all pages
  - Update layout.tsx to wrap the app with the language provider
  - Replace all hard-coded UI strings with t('key') calls
  - Which files to update:
      frontend/src/app/layout.tsx
      frontend/src/context/  (add LanguageContext.tsx)
      ALL page.tsx files that show user-visible text
      frontend/src/components/Sidebar.tsx (navigation labels)

  AGENT NOTE: This is the largest single task. Suggested phased approach:
    Phase 5a — scaffold next-intl, create en.json with all keys, wire provider
    Phase 5b — replace strings in Sidebar + high-traffic pages (dashboard, login)
    Phase 5c — remaining pages (settings, employees, attendance, access-records)


--------------------------------------------------------------------------------
TASK 6  [DONE]  Apply Time Zone Setting to all date/time displays
--------------------------------------------------------------------------------
Files created/updated:
  - frontend/src/lib/formatDateTime.ts  (NEW)
      formatDate(iso, dateFormat, timezone) → string
      formatTime(iso, timeFormat, timezone) → string
      formatDateTime(iso, dateFormat, timeFormat, timezone) → string
      Uses Intl.DateTimeFormat (no extra dependency)
  - frontend/src/app/page.tsx
      formatEventTime / formatEventDate now call formatTime / formatDate
      Dashboard clock respects settings.timeZone
  - frontend/src/context/SettingsContext.tsx  (see TASK 12)
      exposes settings.timeZone consumed by above

  FUTURE: Other pages (attendance, access-records, employees) still use
  raw toLocaleDateString() — replace with formatDate() from this utility.


--------------------------------------------------------------------------------
TASK 7  [DONE]  Apply Date Format Setting
--------------------------------------------------------------------------------
Implemented inside frontend/src/lib/formatDateTime.ts.
Supported formats:
  "DD/MM/YYYY"   "MM/DD/YYYY"   "YYYY-MM-DD"
  "DD-MM-YYYY"   "DD.MM.YYYY"   "D MMM YYYY"   "MMMM D, YYYY"
Dashboard uses dateFormat from SettingsContext when rendering event dates.

  FUTURE: Attendance, access-records and employee pages still use hard-coded
  locale strings — run formatDate() replacement across those pages.


--------------------------------------------------------------------------------
TASK 8  [DONE]  Apply Time Format Setting (12h / 24h)
--------------------------------------------------------------------------------
Implemented inside frontend/src/lib/formatDateTime.ts.
  12h → Intl hour12: true   (e.g. "02:30:05 PM")
  24h → Intl hour12: false  (e.g. "14:30:05")
Dashboard clock and event timestamps both respect settings.timeFormat.


--------------------------------------------------------------------------------
TASK 9  [DONE]  Apply Theme Setting (Light / Dark mode — COMPLETE)
--------------------------------------------------------------------------------
Files updated:
  - frontend/tailwind.config.js  — darkMode: 'class'
  - frontend/src/context/SettingsContext.tsx
      Toggles document.documentElement.classList('dark') when theme changes
  - frontend/src/app/globals.css
      Replaced prefers-color-scheme media query with CSS vars controlled by
      .dark class (background-rgb, foreground-rgb)
  - frontend/src/components/Sidebar.tsx
      Added dark: variants to sidebar panel, nav dropdowns (bg/border/text)
  - ALL page.tsx files (bulk update via PowerShell):
      bg-gradient-to-br → added dark:from-gray-900 dark:to-gray-950
      bg-white panels   → added dark:bg-gray-800
      border-gray-200   → added dark:border-gray-700
      table headers     → added dark:bg-gray-700/60 dark:text-gray-400
      text-gray-900     → added dark:text-gray-100


--------------------------------------------------------------------------------
TASK 10  [DONE]  Apply Company Name Setting
--------------------------------------------------------------------------------
Files updated:
  - frontend/src/app/login/page.tsx
      * Shows settings.companyName || 'AccessPro' in the login card header
  - frontend/src/context/SettingsContext.tsx
      * useEffect sets document.title = "${companyName} — Access Control"
        whenever companyName or isLoaded changes.
        All pages pick this up automatically without any per-page code.


--------------------------------------------------------------------------------
TASK 11  [DONE]  Database Retention Cleanup Scheduler (Backend — SQL-aware)
--------------------------------------------------------------------------------
Files created/updated:
  - backend/src/repositories/IAccessRepository.ts
      * Added abstract deleteOlderThan(cutoff: Date): Promise<number>
  - backend/src/repositories/FileRepository.ts
      * Implemented deleteOlderThan() — filters in-memory array, saves to JSON
  - backend/src/repositories/SqlAccessRecordRepository.ts
      * Implemented deleteOlderThan() — DELETE FROM access_records WHERE swipe_time < ?
  - backend/src/services/retentionCleanupService.ts
      * Refactored: now calls RepositoryFactory.accessRecords().deleteOlderThan(cutoff)
        Works for both JSON-file and SQL backends automatically.
      * Reads dbRetentionMonths from db-config.json via loadRetentionMonths()
        (previously read from general-settings.json — updated in TASK 14)
  - backend/src/server.ts   — calls retentionCleanupService.start()
  - backend/src/routes/settings.ts — runNow() trigger removed (moved to TASK 14)


--------------------------------------------------------------------------------
TASK 12  [DONE]  Frontend: SettingsContext — Global settings provider
--------------------------------------------------------------------------------
Files created/updated:
  - frontend/src/context/SettingsContext.tsx  (NEW)
      * Fetches GET /api/settings/general on mount
      * Exposes: companyName, language, timeZone, dateFormat, timeFormat, theme
        (dbRetentionMonths removed — see TASK 14)
      * Applies document.documentElement.classList('dark') for dark mode
      * refresh() callable — re-fetches and propagates changes
  - frontend/src/app/layout.tsx
      * Wraps app with <SettingsProvider> (inside ConfigProvider, outside Socket)
  - frontend/src/app/settings/general/page.tsx
      * Calls refreshGlobalSettings() after a successful PUT save


--------------------------------------------------------------------------------
TASK 13  [DONE — stub]  Export general settings in system backup / import
--------------------------------------------------------------------------------
Files updated:
  - backend/src/routes/system.ts
      * Added POST /api/system/backup stub (returns 501 Not Implemented)
      * When the backup feature is built, this endpoint should zip all
        backend/data/*.json files (including general-settings.json and
        db-config.json) and return the archive as a download.


--------------------------------------------------------------------------------
TASK 14  [DONE]  Move Data Retention to Database Settings page
--------------------------------------------------------------------------------
Reason: dbRetentionMonths is conceptually database config, not general settings.
Storage: moved from general-settings.json → db-config.json (extra top-level key).

Files updated:
  - backend/src/routes/database-settings.ts
      * loadRetentionMonths() — reads dbRetentionMonths from db-config.json
      * saveRetentionMonths(months) — writes to db-config.json
      * saveConfig() updated to preserve extra fields (e.g. dbRetentionMonths)
        when saving DB connection config
      * GET  /api/database/retention  → { success, dbRetentionMonths }
      * PUT  /api/database/retention  → saves, calls retentionCleanupService.runNow()
  - backend/src/services/retentionCleanupService.ts
      * getRetentionMonths() now imports loadRetentionMonths() from database-settings.ts
        instead of reading general-settings.json directly
  - backend/src/routes/settings.ts
      * Removed dbRetentionMonths from GENERAL_SETTINGS_DEFAULTS
      * Removed dbRetentionMonths from allowed[] fields list
      * Removed retentionCleanupService.runNow() trigger (now in database route)
  - frontend/src/context/SettingsContext.tsx
      * Removed dbRetentionMonths from GeneralSettings interface and DEFAULTS
  - frontend/src/app/settings/general/page.tsx
      * Removed Data Retention section (select + amber note box)
      * Removed dbRetentionMonths from GeneralSettings interface and DEFAULTS
      * Removed DB_RETENTION_OPTIONS constant
  - frontend/src/app/settings/database/page.tsx
      * Added DB_RETENTION_OPTIONS constant
      * Added retentionMonths, isRetentionSaving, retentionSaveResult state
      * loadConfig() also fetches GET /api/database/retention on mount
      * New "Data Retention" card in left column (after action buttons):
          - Select using DB_RETENTION_OPTIONS
          - "Save Retention Settings" button (separate from DB connection save)
          - Success/error feedback inline
      * handleSaveRetention() → PUT /api/database/retention


================================================================================
REMAINING / FUTURE WORK
================================================================================

HIGH PRIORITY
-------------
- TASK 5   (Language/i18n) — largest remaining task; start with en.json scaffold
  Recommended library: next-intl  (install: npm install next-intl)
  Files to create: frontend/src/locales/en.json, bn.json, etc.
  Files to update: layout.tsx, all page.tsx, Sidebar.tsx

MEDIUM PRIORITY
---------------
- Backup feature (Task 13 full implementation):
    backend/src/routes/system.ts  POST /api/system/backup
    Use adm-zip or archiver to zip backend/data/*.json
    Return as octet-stream download with Content-Disposition header
    Add a "Download Backup" button to Settings → System page

- Settings change broadcast via WebSocket:
    When PUT /api/settings/general succeeds, emit 'settings:updated' from
    Socket.IO server. SettingsContext should listen and call refresh()
    so all open browser tabs get the new settings without a page reload.

LOW PRIORITY / FUTURE FEATURES
-------------------------------
- Company name in report/PDF export headers (when PDF reporting is built).
- "Re-apply settings on login": after user logs in, force settings refresh
  so theme/timezone/dateformat are always current.
- Settings import/restore: POST /api/system/restore endpoint that accepts a
  zip backup and restores all JSON data files.

================================================================================
ALL TASKS COMPLETE (except Task 5 Language/i18n)
================================================================================
  [DONE] Task 1   Backend API endpoints
  [DONE] Task 2   Frontend General Settings page UI
  [DONE] Task 3   Settings Index page card
  [DONE] Task 4   Sidebar dropdown entry
  [TODO] Task 5   Language / i18n  (skipped by user request)
  [DONE] Task 6   Time Zone applied across all date/time displays
  [DONE] Task 7   Date Format applied across all date/time displays
  [DONE] Task 8   Time Format applied across all date/time displays
  [DONE] Task 9   Dark mode — class toggle + Tailwind dark: variants on all pages
  [DONE] Task 10  Company Name in login + dynamic browser <title>
  [DONE] Task 11  Retention cleanup scheduler (SQL-aware via repository layer)
  [DONE] Task 12  SettingsContext global provider
  [DONE] Task 13  Backup stub endpoint (POST /api/system/backup)
  [DONE] Task 14  Data Retention moved to Database Settings page (db-config.json)

================================================================================

