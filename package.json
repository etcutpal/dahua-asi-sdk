{
  "name": "asi-sdk-cs",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "kill-ports": "kill-port 3000 3001",
    "backend": "npm start --prefix backend",
    "frontend": "npm start --prefix frontend",
    "dev": "npm run kill-ports"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "kill-port": "^1.6.1"
  }
}